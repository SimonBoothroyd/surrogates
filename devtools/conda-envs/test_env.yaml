channels:
  - gpytorch
  - pytorch
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Main
  - arviz
  - autograd
  - corner
  - matplotlib
  - numpy
  - pandas
  - pydantic
  - pymbar
  - tqdm
  - pyyaml
  - scipy

    # Gaussian Processes
  - pytorch
  - gpytorch
