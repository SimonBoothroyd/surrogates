channels:
  - gpytorch
  - pytorch
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Main
  - arviz
  - corner
  - matplotlib
  - numpy
  - pandas
  - pydantic
  - pymbar
  - tqdm
  - pyyaml

    # Gaussian Processes
  - pytorch
  - gpytorch
